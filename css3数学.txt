1. Animate.css  里面是用贝萨尔曲线做的一个动画库

2. clip: rect(30px,200px,120px,30px);
    /*clip表示元素剪裁,只有一个取值rect
        rect的四个参数:顺序,上右下左
        第一个:重上面开始要裁剪掉的部分
        第二个:重左边开始要保留到的像素位置
        第三个:重上边开始要保留到的像素位置
        第四个:重左边开始要裁剪掉的部分*/
            
3. 矩阵  css变换本质上就是矩阵变换  transform:matrix(a,b,c,d,e,f)